{
  "name": "ojt-projects-portal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "http-server -p 3000 -c-1",
    "build": "echo 'No build required'",
    "preview": "http-server -p 3000 -c-1"
  },
  "devDependencies": {
    "vite": "^5.2.11"
  }
}